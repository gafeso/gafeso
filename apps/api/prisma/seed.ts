/**
 * Seed Prisma — placeholder.
 * Les données de test (tenants de démo, comptes) arriveront avec les modules
 * métier (accounts, tenancy). Pour l'instant le squelette n'ensemence rien.
 */
async function main(): Promise<void> {
  console.log('Aucun seed pour le moment (squelette).');
}

main();
